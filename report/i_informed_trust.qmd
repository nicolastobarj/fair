---
title: "i_informed_trust"
format: html
execute: 
  echo: false
  warning: false
---

# Estrategia 1. Escala cuantitativa Informed Trust

# Distribución informed trust

```{r}

source(here::here("script/proc_data.R"))

library(dplyr)
library(tidyr)
library(ggplot2)
library(sjPlot)

vars_informed <- c(
  "fonasa_conf_belief_funcional_pos_calibrated",
  "fonasa_conf_belief_moral_pos_calibrated",
  "fonasa_conf_word_funcional_pos_calibrated",
  "fonasa_conf_word_moral_pos_calibrated"
)

data_long <- data %>%
  select(all_of(vars_informed)) %>%
  pivot_longer(
    cols = everything(),
    names_to = "tipo_confianza",
    values_to = "valor"
  )

data <- data%>%
  filter(eliminado == 0)
```

```{r}
count(data, get_variable_saec)|>kable()
```

### Compare variables distributions (standariezed)

```{r}
trust_long <- data |>
  transmute(
    FONASA_objective = (fonasa_comp_objetiva_pos - 1) / (11 - 1),
    SAE_objective = (sae_comp_objetiva_pos - 1) / (7 - 1),
    FONASA_functional = (fonasa_conf_belief_funcional_pos - 1) / (7 - 1),
    SAE_functional = (sae_conf_belief_funcional_pos - 1) / (7 - 1),
    FONASA_moral = (fonasa_conf_belief_moral_pos - 1) / (7 - 1),
    SAE_moral = (sae_conf_belief_moral_pos - 1) / (7 - 1)
  ) |>
  pivot_longer(
    cols = everything(),
    names_to = "measure",
    values_to = "score_norm"
  ) |>
  mutate(
    system = case_when(
      grepl("^FONASA", measure) ~ "FONASA",
      grepl("^SAE", measure) ~ "SAE"
    ),
    construct = case_when(
      grepl("objective", measure) ~ "Objective knowledge",
      grepl("functional", measure) ~ "Functional trust",
      grepl("moral", measure) ~ "Moral trust"
    )
  )

ggplot(trust_long, aes(x = score_norm, fill = system)) +
  geom_density(alpha = 0.4, na.rm = TRUE) +
  facet_wrap(~construct) +
  scale_x_continuous(
    labels = scales::percent_format(),
    breaks = seq(0, 1, 0.2)
  ) +
  labs(
    x = "Normalized position in scale",
    y = "Density",
    fill = "System"
  ) +
  theme_minimal()
```

```{r}
ggplot(data_long, aes(x = valor)) +
  geom_histogram(bins = 30, fill = "grey70", color = "white", na.rm = TRUE) +
  facet_wrap(~ tipo_confianza, scales = "free_y") +
  labs(
    x = "Confianza desinformada/Esceptiscismo o desconfianza/Confianza informada",
    y = "Frecuencia"
  ) +
  theme_minimal()

```
```{r}
dv <- "fonasa_conf_belief_funcional_pos_calibrated"

ivs <- c(
  "ai_disclosure_fonasa",
  "control_humano_fonasa",
  "performance_fonasa"
)

library(dplyr)

data %>%
  group_by(ai_disclosure_fonasa) %>%
  summarise(
    mean = mean(.data[[dv]], na.rm = TRUE),
    sd   = sd(.data[[dv]], na.rm = TRUE),
    n    = n()
  )


data %>%
  group_by(performance_fonasa) %>%
  summarise(
    mean = mean(.data[[dv]], na.rm = TRUE),
    sd   = sd(.data[[dv]], na.rm = TRUE),
    n    = n()
  )


data %>%
  group_by(control_humano_fonasa) %>%
  summarise(
    mean = mean(.data[[dv]], na.rm = TRUE),
    sd   = sd(.data[[dv]], na.rm = TRUE),
    n    = n()
  )
```

```{r}
dv <- "fonasa_conf_belief_moral_pos_calibrated"

data %>%
  group_by(ai_disclosure_fonasa) %>%
  summarise(
    mean = mean(.data[[dv]], na.rm = TRUE),
    sd   = sd(.data[[dv]], na.rm = TRUE),
    n    = n()
  )


data %>%
  group_by(performance_fonasa) %>%
  summarise(
    mean = mean(.data[[dv]], na.rm = TRUE),
    sd   = sd(.data[[dv]], na.rm = TRUE),
    n    = n()
  )


data %>%
  group_by(control_humano_fonasa) %>%
  summarise(
    mean = mean(.data[[dv]], na.rm = TRUE),
    sd   = sd(.data[[dv]], na.rm = TRUE),
    n    = n()
  )

```


```{r}
dv <- "fonasa_conf_belief_funcional_pos_calibrated"

ggplot(data, aes(
  x = factor(ai_disclosure_fonasa),
  y = .data[[dv]]
)) +
  geom_boxplot(outlier.shape = NA, alpha = 0.6) +
  geom_jitter(width = 0.1, alpha = 0.3) +
  labs(
    x = "AI disclosure",
    y = "Calibrated trust"
  ) +
  theme_minimal()

```
```{r}
ggplot(data, aes(
  x = factor(performance_fonasa),
  y = .data[[dv]]
)) +
  geom_boxplot(outlier.shape = NA, alpha = 0.6) +
  geom_jitter(width = 0.1, alpha = 0.3) +
  labs(
    x = "Performance",
    y = "Calibrated trust"
  ) +
  theme_minimal()
```


```{r}
ggplot(data, aes(
  x = factor(control_humano_fonasa),
  y = .data[[dv]]
)) +
  geom_boxplot(outlier.shape = NA, alpha = 0.6) +
  geom_jitter(width = 0.1, alpha = 0.3) +
  labs(
    x = "Human control",
    y = "Calibrated trust"
  ) +
  theme_minimal()
```

```{r}
dv <- "fonasa_conf_belief_moral_pos_calibrated"

ggplot(data, aes(
  x = factor(ai_disclosure_fonasa),
  y = .data[[dv]]
)) +
  geom_boxplot(outlier.shape = NA, alpha = 0.6) +
  geom_jitter(width = 0.1, alpha = 0.3) +
  labs(
    x = "AI disclosure",
    y = "Calibrated trust"
  ) +
  theme_minimal()

```
```{r}
ggplot(data, aes(
  x = factor(performance_fonasa),
  y = .data[[dv]]
)) +
  geom_boxplot(outlier.shape = NA, alpha = 0.6) +
  geom_jitter(width = 0.1, alpha = 0.3) +
  labs(
    x = "Performance",
    y = "Calibrated trust"
  ) +
  theme_minimal()
```


```{r}
ggplot(data, aes(
  x = factor(control_humano_fonasa),
  y = .data[[dv]]
)) +
  geom_boxplot(outlier.shape = NA, alpha = 0.6) +
  geom_jitter(width = 0.1, alpha = 0.3) +
  labs(
    x = "Human control",
    y = "Calibrated trust"
  ) +
  theme_minimal()
```


```{r}
models_total <- list(
  belief_funcional = lm(as.formula(paste("fonasa_conf_belief_funcional_pos_calibrated", "~ ai_disclosure_fonasa + control_humano_fonasa + performance_fonasa + genero + edad_num + education_recoded")), data = data),
  belief_moral = lm(as.formula(paste("fonasa_conf_belief_moral_pos_calibrated", "~ ai_disclosure_fonasa + control_humano_fonasa + performance_fonasa + genero + edad_num + education_recoded")), data = data),
  word_funcional = lm(as.formula(paste("fonasa_conf_word_funcional_pos_calibrated", "~ ai_disclosure_fonasa + control_humano_fonasa + performance_fonasa + genero + edad_num + education_recoded")), data = data),
  word_moral = lm(as.formula(paste("fonasa_conf_word_moral_pos_calibrated", "~ ai_disclosure_fonasa + control_humano_fonasa + performance_fonasa + genero + edad_num + education_recoded")), data = data)
)

tab_model(
  models_total,
  dv.labels = c("Calibrated functional McKnight trust (FONASA)","Calibrated moral McKnight trust (FONASA)","Calibrated functional MDMT trust (FONASA)","Calibrated moral MDMT trust (FONASA)"),
  pred.labels = c(
    "(Intercept)",
    "AI disclosure",
    "Human control",
    "Performance",
    "Genero: Mujer",
    "Edad",
    "Educación: Media completa",
    "Educación: Superior"
  ),
    collapse.se = TRUE,      
  show.ci = FALSE,
  show.aic = FALSE,
  show.r2 = TRUE,
  show.fstat = TRUE,
  p.style = "stars"
)
```

# SAE

## Modelo Informed Trust continuos

```{r}
models_total <- list(
  belief_funcional = lm(
    as.formula(paste(
      "sae_conf_belief_funcional_pos_calibrated",
      "~ disclosure + inf_situacional + control_humano + genero + edad_num + education_recoded"
    )),
    data = data
  ),
  belief_moral = lm(
    as.formula(paste(
      "sae_conf_belief_moral_pos_calibrated",
      "~ disclosure + inf_situacional + control_humano + genero + edad_num + education_recoded"
    )),
    data = data
  ),
  word_funcional = lm(
    as.formula(paste(
      "sae_conf_word_funcional_pos_calibrated",
      "~ disclosure + inf_situacional + control_humano + genero + edad_num + education_recoded"
    )),
    data = data
  ),
  word_moral = lm(
    as.formula(paste(
      "sae_conf_word_moral_pos_calibrated",
      "~ disclosure + inf_situacional + control_humano + genero + edad_num + education_recoded"
    )),
    data = data
  )
)

tab_model(
  models_total,
  dv.labels = c(
    "Calibrated functional McKnight trust (SAE)",
    "Calibrated moral McKnight trust (SAE)",
    "Calibrated functional MDMT trust (SAE)",
    "Calibrated moral MDMT trust (SAE)"
  ),
  pred.labels = c(
    "(Intercept)",
    "Disclosure",
    "Situational information",
    "Human control",
    "Genero: Mujer",
    "Edad",
    "Educación: Media completa",
    "Educación: Superior"
  ),
  collapse.se = TRUE,
  show.ci = FALSE,
  show.aic = FALSE,
  show.r2 = TRUE,
  show.fstat = TRUE,
  p.style = "stars"
)
```

```{r}
models_total <- list(
  belief_funcional = lm(
    as.formula(paste(
      "sae_conf_belief_funcional_pos_calibrated",
      "~ disclosure*attitudes_ai + inf_situacional*attitudes_ai + control_humano*attitudes_ai + genero + edad_num + education_recoded"
    )),
    data = data
  ),
  belief_moral = lm(
    as.formula(paste(
      "sae_conf_belief_moral_pos_calibrated",
      "~ disclosure*attitudes_ai + inf_situacional*attitudes_ai + control_humano*attitudes_ai + genero + edad_num + education_recoded"
    )),
    data = data
  ),
  word_funcional = lm(
    as.formula(paste(
      "sae_conf_word_funcional_pos_calibrated",
      "~ disclosure*attitudes_ai + inf_situacional*attitudes_ai + control_humano*attitudes_ai + genero + edad_num + education_recoded"
    )),
    data = data
  ),
  word_moral = lm(
    as.formula(paste(
      "sae_conf_word_moral_pos_calibrated",
      "~ disclosure*attitudes_ai + inf_situacional*attitudes_ai + control_humano*attitudes_ai + genero + edad_num + education_recoded"
    )),
    data = data
  )
)

tab_model(
  models_total,
  dv.labels = c(
    "Calibrated functional McKnight trust (SAE)",
    "Calibrated moral McKnight trust (SAE)",
    "Calibrated functional MDMT trust (SAE)",
    "Calibrated moral MDMT trust (SAE)"
  ),
  collapse.se = TRUE,
  show.ci = FALSE,
  show.aic = FALSE,
  show.r2 = TRUE,
  show.fstat = TRUE,
  p.style = "stars"
)
```

```{r}
plot_model(
  lm(
    sae_conf_belief_funcional_pos_calibrated ~
      attitudes_ai *
      disclosure +
      inf_situacional * attitudes_ai +
      control_humano * attitudes_ai +
      genero +
      edad_num +
      education_recoded,
    data = data
  ),
  terms = c("control_humano", "attitudes_ai"),
  type = "int"
)
```


# Estrategia 2. Tricotomizar y modelo multinomial

## Construcción de la variable

- Normalización de las variables de conocimiento y confianza (z-scores)
- Clasificación en categorías: z > 0, z <= 0.
- Creación de categorías combinadas para cada sistema, con base en la combinación.
- Fundición de categorías con baja confianza para ganar poder estadístico.

```{r}
library(nnet)

data <- data %>%
  mutate(
    z_know_fonasa = as.numeric(scale(fonasa_comp_objetiva_pos)),
    z_trust_func_fonasa = as.numeric(scale(fonasa_conf_belief_funcional_pos)),
    z_trust_moral_fonasa = as.numeric(scale(fonasa_conf_belief_moral_pos))
  )

data <- data %>%
  mutate(
    fonasa_calibrated_misinfo_func = case_when(
      z_know_fonasa >= 0 & z_trust_func_fonasa >= 0 ~ "Informed trust",
      z_know_fonasa < 0 & z_trust_func_fonasa >= 0 ~ "Ignorant trust",
      z_trust_func_fonasa < 0 ~ "Skeptical informed / mistrust uninformed"
    )
  )

data <- data %>%
  mutate(
    fonasa_calibrated_misinfo_moral = case_when(
      z_know_fonasa >= 0 & z_trust_moral_fonasa >= 0 ~ "Informed trust",
      z_know_fonasa < 0 & z_trust_moral_fonasa >= 0 ~ "Ignorant trust",
      z_trust_moral_fonasa < 0 ~ "Skeptical informed / mistrust uninformed"
    )
  )

data <- data %>%
  mutate(
    fonasa_calibrated_funcional_cat = relevel(
      factor(fonasa_calibrated_misinfo_func),
      ref = "Skeptical informed / mistrust uninformed"
    ),
    fonasa_calibrated_moral_cat = relevel(
      factor(fonasa_calibrated_misinfo_moral),
      ref = "Skeptical informed / mistrust uninformed"
    )
  )

ivs_intervencion <- c(
  "ai_disclosure_fonasa",
  "control_humano_fonasa",
  "performance_fonasa"
)

ivs_demograficas <- c(
  "genero",
  "edad_num",
  "education_recoded"
)
```

```{r}
count(data,fonasa_calibrated_funcional_cat)%>%
  mutate(prop = round(n / sum(n),2)) %>%
  kable()

count(data,fonasa_calibrated_moral_cat)%>%
  mutate(prop = round(n / sum(n),2)) %>%
  kable()
```

```{r}
library(scales)

vars_interes <- c(
  "ai_disclosure_fonasa",
  "control_humano_fonasa",
  "performance_fonasa"
)

etiquetas_vars <- c(
  ai_disclosure_fonasa = "AI disclosure",
  control_humano_fonasa = "Human control",
  performance_fonasa = "Performance"
)

plot_data_func <- data |>
  select(fonasa_calibrated_funcional_cat, all_of(vars_interes)) |>
  pivot_longer(
    cols = all_of(vars_interes),
    names_to = "variable",
    values_to = "valor"
  ) |>
  count(fonasa_calibrated_funcional_cat, variable, valor) |>
  group_by(fonasa_calibrated_funcional_cat, variable) |>
  mutate(
    prop = n / sum(n),
    label = percent(prop, accuracy = 1)
  ) |>
  ungroup() |>
  mutate(
    variable = recode(variable, !!!etiquetas_vars),
    valor = factor(valor, levels = c(0, 1))
  )

p_func <- ggplot(
  plot_data_func,
  aes(
    y = variable,
    x = prop,
    fill = factor(valor)
  )
) +
  geom_col() +
  geom_text(
    aes(label = label),
    position = position_stack(vjust = 0.5),
    size = 3
  ) +
  facet_wrap(~ fonasa_calibrated_funcional_cat, ncol = 1) +
  scale_x_continuous(labels = percent_format()) +
  labs(
    title = "Informed Functional trust",
    x = "Percentage",
    y = NULL,
    fill = "Value"
  ) +
  theme_minimal()

plot_data_moral <- data |>
  select(fonasa_calibrated_moral_cat, all_of(vars_interes)) |>
  pivot_longer(
    cols = all_of(vars_interes),
    names_to = "variable",
    values_to = "valor"
  ) |>
  count(fonasa_calibrated_moral_cat, variable, valor) |>
  group_by(fonasa_calibrated_moral_cat, variable) |>
  mutate(
    prop = n / sum(n),
    label = percent(prop, accuracy = 1)
  ) |>
  ungroup() |>
  mutate(
    variable = recode(variable, !!!etiquetas_vars),
    valor = factor(valor, levels = c(0, 1))
  )

p_moral <- ggplot(
  plot_data_moral,
  aes(
    y = variable,
    x = prop,
    fill = factor(valor)
  )
) +
  geom_col() +
  geom_text(
    aes(label = label),
    position = position_stack(vjust = 0.5),
    size = 3
  ) +
  facet_wrap(~ fonasa_calibrated_moral_cat, ncol = 1) +
  scale_x_continuous(labels = percent_format()) +
  labs(
    title = "Informed Moral trust",
    x = "Percentage",
    y = NULL,
    fill = "Value"
  ) +
  theme_minimal()

p_func / p_moral
```

```{r}
tab_model(
  multinom(
    fonasa_calibrated_funcional_cat ~
      ai_disclosure_fonasa +
      control_humano_fonasa +
      performance_fonasa +
      genero +
      edad_num +
      education_recoded +
      attitudes_ai +
      ai_skills,
    data = data
  ),
  dv.labels = c(
    "Informed Trust – Functional (FONASA, post)"
  ),
  show.ci = FALSE,
  p.style = "stars",
  pred.labels = c(
    "(Intercept)",
    "AI disclosure",
    "Human control",
    "System performance",
    "Gender (Women = 1)",
    "Age",
    "Educational level",
    "Attitudes towards AI",
    "AI skills"
  )
)

tab_model(
  multinom(
    fonasa_calibrated_moral_cat ~
      ai_disclosure_fonasa +
      control_humano_fonasa +
      performance_fonasa +
      genero +
      edad_num +
      education_recoded +
      attitudes_ai +
      ai_skills,
    data = data
  ),
  dv.labels = c(
    "Informed Trust – Moral (FONASA, post)"
  ),
  show.ci = FALSE,
  p.style = "stars",
  pred.labels = c(
    "(Intercept)",
    "AI disclosure",
    "Human control",
    "System performance",
    "Gender (Women = 1)",
    "Age",
    "Educational level",
    "Attitudes towards AI",
    "AI skills"
  )
)
```

```{r}
data <- data %>%
  mutate(
    z_know_sae = as.numeric(scale(sae_comp_objetiva_pos)),
    z_trust_func_sae = as.numeric(scale(sae_conf_belief_funcional_pos)),
    z_trust_moral_sae = as.numeric(scale(sae_conf_belief_moral_pos))
  )

data <- data %>%
  mutate(
    sae_calibrated_misinfo_func = case_when(
      z_know_sae >= 0 & z_trust_func_sae >= 0 ~ "Informed trust",
      z_know_sae < 0 & z_trust_func_sae >= 0 ~ "Ignorant trust",
      z_trust_func_sae < 0 ~ "Skeptical informed / mistrust uninformed"
    )
  )

data <- data %>%
  mutate(
    sae_calibrated_misinfo_moral = case_when(
      z_know_sae >= 0 & z_trust_moral_sae >= 0 ~ "Informed trust",
      z_know_sae < 0 & z_trust_moral_sae >= 0 ~ "Ignorant trust",
      z_trust_moral_sae < 0 ~ "Skeptical informed / mistrust uninformed"
    )
  )

data <- data %>%
  mutate(
    sae_calibrated_funcional_cat = relevel(
      factor(sae_calibrated_misinfo_func),
      ref = "Skeptical informed / mistrust uninformed"
    ),
    sae_calibrated_moral_cat = relevel(
      factor(sae_calibrated_misinfo_moral),
      ref = "Skeptical informed / mistrust uninformed"
    )
  )

ivs_intervencion <- c(
  "disclosure",
  "control_humano",
  "inf_situacional"
)

ivs_demograficas <- c(
  "genero",
  "edad_num",
  "education_recoded"
)
```

```{r}
count(data, sae_calibrated_funcional_cat) %>%
  mutate(prop = round(n / sum(n), 2)) %>%
  kable()
count(data, sae_calibrated_moral_cat) %>%
  mutate(prop = round(n / sum(n), 2)) %>%
  kable()
```

```{r}
library(scales)
library(patchwork)

vars_interes <- c(
  "disclosure",
  "control_humano",
  "inf_situacional"
)

etiquetas_vars <- c(
  disclosure = "Disclosure",
  control_humano = "Human control",
  inf_situacional = "Situational information"
)

plot_data_func <- data |>
  select(sae_calibrated_funcional_cat, all_of(vars_interes)) |>
  pivot_longer(
    cols = all_of(vars_interes),
    names_to = "variable",
    values_to = "valor"
  ) |>
  count(sae_calibrated_funcional_cat, variable, valor) |>
  group_by(sae_calibrated_funcional_cat, variable) |>
  mutate(
    prop = n / sum(n),
    label = percent(prop, accuracy = 1)
  ) |>
  ungroup() |>
  mutate(
    variable = recode(variable, !!!etiquetas_vars),
    valor = factor(valor, levels = c(0, 1))
  )

p_func <- ggplot(
  plot_data_func,
  aes(
    y = variable,
    x = prop,
    fill = valor
  )
) +
  geom_col() +
  geom_text(
    aes(label = label),
    position = position_stack(vjust = 0.5),
    size = 3
  ) +
  facet_wrap(~sae_calibrated_funcional_cat, ncol = 1) +
  scale_x_continuous(labels = percent_format()) +
  labs(
    title = "Informed Functional trust",
    x = "Percentage",
    y = NULL,
    fill = "Value"
  ) +
  theme_minimal()

plot_data_moral <- data |>
  select(sae_calibrated_moral_cat, all_of(vars_interes)) |>
  pivot_longer(
    cols = all_of(vars_interes),
    names_to = "variable",
    values_to = "valor"
  ) |>
  count(sae_calibrated_moral_cat, variable, valor) |>
  group_by(sae_calibrated_moral_cat, variable) |>
  mutate(
    prop = n / sum(n),
    label = percent(prop, accuracy = 1)
  ) |>
  ungroup() |>
  mutate(
    variable = recode(variable, !!!etiquetas_vars),
    valor = factor(valor, levels = c(0, 1))
  )

p_moral <- ggplot(
  plot_data_moral,
  aes(
    y = variable,
    x = prop,
    fill = valor
  )
) +
  geom_col() +
  geom_text(
    aes(label = label),
    position = position_stack(vjust = 0.5),
    size = 3
  ) +
  facet_wrap(~sae_calibrated_moral_cat, ncol = 1) +
  scale_x_continuous(labels = percent_format()) +
  labs(
    title = "Informed Moral trust",
    x = "Percentage",
    y = NULL,
    fill = "Value"
  ) +
  theme_minimal()

p_func / p_moral
```

```{r}
tab_model(
  multinom(
    sae_calibrated_funcional_cat ~
      disclosure +
      control_humano +
      inf_situacional +
      genero +
      edad_num +
      education_recoded +
      attitudes_ai +
      ai_skills,
    data = data
  ),
  dv.labels = c(
    "Informed Trust – Functional (SAE, post)"
  ),
  show.ci = FALSE,
  p.style = "stars",
  pred.labels = c(
    "(Intercept)",
    "Disclosure",
    "Human control",
    "Situational information",
    "Gender (Women = 1)",
    "Age",
    "Educational level",
    "Attitudes towards AI",
    "AI skills"
  )
)

tab_model(
  multinom(
    sae_calibrated_moral_cat ~
      disclosure +
      control_humano +
      inf_situacional +
      genero +
      edad_num +
      education_recoded +
      attitudes_ai +
      ai_skills,
    data = data
  ),
  dv.labels = c(
    "Informed Trust – Moral (SAE, post)"
  ),
  show.ci = FALSE,
  p.style = "stars",
  pred.labels = c(
    "(Intercept)",
    "AI disclosure",
    "Human control",
    "System performance",
    "Gender (Women = 1)",
    "Age",
    "Educational level",
    "Attitudes towards AI",
    "AI skills"
  )
)
```

### Sistema como control (Multinomial con ambos sistemas)

```{r}
# R
# Construir constructos (media de ítems) para FONASA y SAE
library(dplyr)

# detectar ítems (si no existen ya los vectores items_...)
items_fonasa_trust <- names(data)[grepl(
  "^fonasa_pos_trust_belief.*_[0-9]+$",
  names(data)
)]
items_sae_trust <- names(data)[grepl(
  "^sae_pos_trust_belief.*_[0-9]+$",
  names(data)
)]

# Umbral mínimo de ítems respondidos (50%)
min_prop <- 0.5

# matrices de ítems
mat_fonasa <- data |> select(all_of(items_fonasa_trust))
mat_sae <- data |> select(all_of(items_sae_trust))

# conteo no-missing y media condicionada
req_fonasa <- ceiling(ncol(mat_fonasa) * min_prop)
req_sae <- ceiling(ncol(mat_sae) * min_prop)

data <- data |>
  mutate(
    fonasa_trust_n = rowSums(!is.na(mat_fonasa)),
    fonasa_trust_mean = if_else(
      fonasa_trust_n >= req_fonasa,
      rowMeans(mat_fonasa, na.rm = TRUE),
      NA_real_
    ),
    sae_trust_n = rowSums(!is.na(mat_sae)),
    sae_trust_mean = if_else(
      sae_trust_n >= req_sae,
      rowMeans(mat_sae, na.rm = TRUE),
      NA_real_
    )
  )

# Resúmenes útiles
resumen_constructos <- tibble(
  construct = c("fonasa_trust_mean", "sae_trust_mean"),
  n_nonmissing = c(
    sum(!is.na(data$fonasa_trust_mean)),
    sum(!is.na(data$sae_trust_mean))
  ),
  mean = c(
    mean(data$fonasa_trust_mean, na.rm = TRUE),
    mean(data$sae_trust_mean, na.rm = TRUE)
  ),
  median = c(
    median(data$fonasa_trust_mean, na.rm = TRUE),
    median(data$sae_trust_mean, na.rm = TRUE)
  ),
  sd = c(
    sd(data$fonasa_trust_mean, na.rm = TRUE),
    sd(data$sae_trust_mean, na.rm = TRUE)
  )
)

resumen_constructos |> kable()
```


```{r}
tab_model(
  multinom(
    sae_calibrated_moral_cat ~
      disclosure +
      control_humano +
      inf_situacional +
      genero +
      edad_num +
      education_recoded +
      attitudes_ai +
      ai_skills,
    data = data
  ),
  
  dv.labels = c(
    "Informed Trust SAE",
    "Informed Trust FONASA"
  ),
  show.ci = FALSE,
  p.style = "stars",
  pred.labels = c(
    "(Intercept)",
    "AI disclosure",
    "Human control",
    "System performance",
    "Gender (Women = 1)",
    "Age",
    "Educational level",
    "Attitudes towards AI",
    "AI skills"
  )
)
```